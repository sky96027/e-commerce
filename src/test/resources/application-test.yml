spring:
  jpa:
    hibernate:
      ddl-auto: create-drop
    open-in-view: false
    properties:
      hibernate.format_sql: true
  data:
    redis:
      host: localhost
      port: 6379
    cache:
      type: redis
logging:
  level:
    org.hibernate.SQL: warn